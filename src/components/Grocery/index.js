import MessageCard from "../MessageCard";

const Grocery = () => {
    return (
        <MessageCard>
            Sample component to lazy load this page :)
        </MessageCard>
    );
}

export default Grocery;